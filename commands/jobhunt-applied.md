Mark the job with id: $ARGUMENTS as applied.

Call `jobhunt_applied(job_id="$ARGUMENTS")` and show the confirmation.
If $ARGUMENTS is empty, ask for the job id first.
If the user mentioned how they applied or a referral, pass it as `notes=`.
