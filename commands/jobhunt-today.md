Pull fresh jobs from all ATS feeds and show today's top matches.

Use the `jobhunt_pull_feed` MCP tool, then call `jobhunt_today`.
Show results sorted by score descending, with TC range and apply URL for each.
Flag roles where LinkedIn referrals are available.
