Browse ALL stored jobs in the tracker, not just today's new arrivals.

Call `jobhunt_search` with whatever filters the user gives in $ARGUMENTS (keyword,
company, min_score, status, page). Default to `min_score=60` for strong matches.
Show score, TC, location, and apply URL per role, plus the pagination hint.
