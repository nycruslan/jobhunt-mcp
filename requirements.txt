# JobHunt MCP — runtime dependencies (Python 3.11)
# Pinned to versions verified working June 2026. Bump deliberately.

requests==2.34.2        # all ATS feed HTTP calls
PyYAML==6.0.3           # targets.yaml + resume/profile.yaml
fpdf2==2.8.7            # resume PDF rendering (pure Python, no system deps)
python-jobspy==1.1.82   # Indeed/Google supplemental scrape (FAANG without clean APIs)
pandas==2.3.3           # JobSpy returns a DataFrame
mcp==1.27.1             # FastMCP server (Claude tool interface)

# Optional — only needed to enable API-side resume/cover tailoring.
# Everything degrades gracefully without it.
anthropic==0.104.0
